import { createSlice } from "@reduxjs/toolkit";

let initialState = {
  count: 0,
};

let prodSlice = createSlice({
  name: "prod",
  initialState,
  reducers: {
   
   
  },
});

export const {} = prodSlice.actions;
export default prodSlice.reducer