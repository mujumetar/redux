
export const incCount = () => {
  return { type: INC_COUNT };
};

export const decCount = () => {
  return { type: DESC_COUNT };
};

export const INC_COUNT = "INC";
export const DESC_COUNT = "DESC";

